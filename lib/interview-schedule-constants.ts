/** Client-safe schedule limits (no Node / db imports). */

/** Max candidates in one group interview (manual schedule + auto-gen). */
export const MAX_INTERVIEW_GROUP_SIZE = 12;
export const MIN_INTERVIEW_GROUP_SIZE = 2;
